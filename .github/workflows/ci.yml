name: CI
on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  test:
    name: tests & lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.8"

      - name: Sync deps
        run: uv sync

      - name: Install dev tools
        run: uv tool install ruff pyright pytest

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format (check)
        run: ruff format --check .

      - name: Pyright
        run: pyright

      - name: Pytest
        run: pytest -q